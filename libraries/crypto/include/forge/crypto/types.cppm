module;

#include <cstddef>
#include <cstdint>
#include <vector>

export module forge.crypto.types;

export namespace forge::crypto {

using bytes = std::vector<std::uint8_t>;

} // namespace forge::crypto
