module;
#include <cstdint>
#include <vector>

export module forge.multiformats.types;

export namespace forge::multiformats {

using bytes = std::vector<std::uint8_t>;

} // namespace forge::multiformats
